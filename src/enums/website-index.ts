/**
 * Website index for multi-site support in Shopier dashboard
 * Values 1-5 correspond to different sites configured in Shopier
 * @enum {number}
 */
export enum WebsiteIndex {
  SITE_1 = 1,
  SITE_2 = 2,
  SITE_3 = 3,
  SITE_4 = 4,
  SITE_5 = 5
}
