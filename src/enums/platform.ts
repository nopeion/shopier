/**
 * Platform display modes for Shopier payment page
 * @enum {number}
 */
export enum PlatformType {
  /** Payment page displayed inside an iframe */
  IN_FRAME = 0,
  /** Payment page displayed in a new window/tab */
  NOT_IN_FRAME = 1
}
